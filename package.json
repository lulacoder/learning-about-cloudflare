{
  "name": "alchemy-effect-durable",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "web:build": "bun run --cwd web build",
    "infra:plan": "bun run web:build && bun alchemy plan --stage preview --env-file .env.preview",
    "infra:dev": "bun alchemy dev --stage dev",
    "infra:deploy": "bun run web:build && bun alchemy deploy --stage preview --env-file .env.preview",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@effect/platform-bun": "rc",
    "@effect/platform-node": "rc",
    "alchemy": "^2.0.0-beta.79",
    "better-auth": "1.7.6",
    "effect": "rc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260923.1",
    "typescript": "^7.0.2"
  }
}
